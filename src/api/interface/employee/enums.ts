
// 员工状态
export const enum EmployeeStatusEnum {
  Normal = 0,
  Stop = 1,
}

// 职位层级
export const enum JobLevelEnum {
  Headquarters = 0, // 总部
  Store = 1,        // 门店
}

// 职位状态
export const enum JobStatusEnum {
  Normal = 0,
  Stop = 1,
}

// 用户角色
export const enum RoleEnum {
  SuperAdmin = 0,      // 超级管理员
  Admin = 1, // 管理员
  User = 2,  // 成员
}
